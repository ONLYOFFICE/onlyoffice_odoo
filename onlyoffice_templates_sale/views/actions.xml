<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="action_doc_sale_from_template" model="ir.actions.server">
            <field name="name">Create doc from template</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="binding_model_id" ref="sale.model_sale_order"/>
            <field name="binding_view_types">form</field>
            <field name="state">code</field>
            <field name="code">
                if records:
                    action = records.action_create_doc_template()
            </field>
        </record>

        <record id="action_doc_lead_from_template" model="ir.actions.server">
            <field name="name">Create doc from template</field>
            <field name="model_id" ref="crm.model_crm_lead"/>
            <field name="binding_model_id" ref="crm.model_crm_lead"/>
            <field name="binding_view_types">form</field>
            <field name="state">code</field>
            <field name="code">
                if records:
                    action = records.action_create_doc_template()
            </field>
        </record>

    </data>
</odoo>